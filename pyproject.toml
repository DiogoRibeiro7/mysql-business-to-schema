[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mysql-business-to-schema"
version = "0.1.0"
description = "Business narratives to production-ready MySQL schemas with generators"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "Faker==24.11.0",
  "PyYAML==6.0.2"
]

[tool.setuptools]
packages = []
